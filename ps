The command ps means "process status" and will find the PID# of all the processes you have launched. Ps x will show all the processes running on the computer. You can specify what programs to look for by using a pipe. $ps x | grep README will find all the programs running that are README.
